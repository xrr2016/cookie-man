export const clear = () => {
  document.cookie = ''
}

export const getItem = key => {}

export const key = n => {}

export const length = () => {}

export const removeItem = key => {}

const _opts = {}

export const setItem = (key, value, opts = _opts) => {}
